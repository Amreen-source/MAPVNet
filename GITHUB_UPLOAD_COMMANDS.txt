# 1. Review and replace unresolved experimental files first.
cd /path/to/MAPVNet_Publication_Candidate

# 2. Audit.
./scripts/pre_release_audit.sh

# 3. Git.
git init
git branch -M main
git add .
git status
git commit -m "MAPVNet publication release"

# If the GitHub repository already exists:
git remote add origin https://github.com/Amreen-source/MAPVNet.git
git push -u origin main --force-with-lease

# Tag only after reproducibility verification:
git tag -a v1.0.0 -m "MAPVNet paper release"
git push origin v1.0.0
