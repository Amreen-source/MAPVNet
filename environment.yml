name: mapvnet
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch==2.5.1
      - torchvision
      - transformers==4.49.0
      - timm
      - albumentations
      - anomalib
      - rasterio
      - folium
      - scikit-learn
      - numpy
      - Pillow
      - matplotlib
      - tqdm
      - einops
      - langgraph
      - langchain
      - bitsandbytes
      - accelerate
      - opencv-python
      - pyyaml
